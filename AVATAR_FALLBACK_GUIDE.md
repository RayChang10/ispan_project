# 虛擬人降級功能指南

## 功能概述

系統現在支援智能降級功能，當 LiveTalking 虛擬人服務無法啟動時，面試系統會自動切換到**純對話模式**，確保面試功能完全正常運作。

## 功能特色

### 🎯 智能降級
- **自動檢測**：系統會自動檢測虛擬人服務狀態
- **無縫切換**：失敗時自動切換到純對話模式
- **用戶體驗優先**：確保對話功能不受影響

### 💬 純對話模式
- **完整功能**：所有面試功能都正常運作
- **優化布局**：對話區域自動擴大，提供更好的體驗
- **一鍵重試**：提供重試按鈕，可嘗試重新連接虛擬人服務

### 🔄 恢復機制
- **重試功能**：用戶可隨時點擊「重試連接」按鈕
- **狀態指示**：清楚顯示當前服務狀態
- **友善提示**：提供明確的操作指引

## 使用場景

### 1. 虛擬人服務未啟動
當 LiveTalking 服務（端口 8010）未運行時：
- 系統自動顯示「純對話模式」界面
- 對話功能完全正常
- 用戶可繼續進行面試

### 2. 服務中斷
如果服務在使用過程中中斷：
- 自動切換到降級模式
- 保存現有對話內容
- 提供重新連接選項

### 3. 網絡問題
當網絡連接不穩定時：
- 優雅處理連接失敗
- 避免影響核心面試功能
- 提供手動重試機制

## 技術實現

### 前端降級邏輯
```javascript
// 自動檢測服務狀態
if (error.includes('502') || error.includes('503') || error.includes('unavailable')) {
    // 切換到降級模式
    enableFallbackMode();
}
```

### 布局自適應
```javascript
// 優化布局，擴大對話區域
avatarSection.style.flex = '0 0 300px';
chatSection.style.flex = '1';
```

### 重試機制
```javascript
// 用戶可主動重試連接
function retryConnection() {
    // 重新嘗試連接虛擬人服務
    start();
}
```

## 啟動選項

### 標準模式（默認）
```bash
python -m backend.main --mode integrated
```
包含虛擬人功能，失敗時自動降級

### 純對話模式
```bash
python -m backend.main --mode chat-only
```
直接以純對話模式啟動，不載入虛擬人組件

### 快捷啟動
```bash
./start_chat_only.sh
```
使用提供的腳本快速啟動純對話模式

## 配置說明

### 環境變數
- `CHAT_ONLY_MODE=true`：強制純對話模式
- `DISABLE_AVATAR=true`：停用虛擬人功能

### 服務端點
- **主要服務**：http://localhost:8080
- **虛擬人服務**：http://localhost:8010（可選）
- **對話 API**：http://localhost:5000

## 用戶體驗

### 視覺指示
- **正常模式**：顯示虛擬人視頻區域
- **降級模式**：顯示友善的佔位符和說明
- **狀態提示**：清楚標示當前服務狀態

### 操作流程
1. 用戶打開面試頁面
2. 系統自動檢測虛擬人服務
3. 如果檢測失敗，自動切換到純對話模式
4. 顯示友善提示，說明情況
5. 用戶可正常進行面試
6. 隨時可點擊「重試連接」恢復虛擬人功能

## 問題排除

### Q: 虛擬人服務無法啟動怎麼辦？
A: 系統會自動切換到純對話模式，所有面試功能都能正常使用。

### Q: 如何重新啟用虛擬人功能？
A: 確保 LiveTalking 服務在端口 8010 運行，然後點擊「重試連接」按鈕。

### Q: 純對話模式是否影響面試功能？
A: 不會，所有面試功能（自我介紹、問答、評分、總結）都完全正常。

### Q: 如何檢查虛擬人服務狀態？
A: 可訪問 http://localhost:8010 檢查 LiveTalking 服務是否運行。

### Q: 看到 "LT.speak 尚未可用" 警告怎麼辦？
A: 這是正常現象，表示朗讀功能因虛擬人服務不可用而被停用。對話功能不受影響。

### Q: 朗讀功能無法使用？
A: 朗讀功能依賴虛擬人服務，當服務不可用時會自動停用。這是保護性措施，不影響核心面試功能。

### Q: 如何測試降級功能？
A: 使用提供的測試腳本：
```bash
python test_fallback.py
```

## 總結

這個降級功能確保了系統的高可用性，讓用戶即使在虛擬人服務不可用的情況下，也能完整體驗所有面試功能。這是一個以用戶體驗為優先的設計，體現了軟件的容錯性和可用性。
