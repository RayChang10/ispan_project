

## MCP 對照與 MVP 計畫（結構化）

### 對照結論（重點）
- **MCP Resources**：未實作（缺 `@mcp.resource`、`resource://resume/*`、`resource://db/*`）
- **MCP Tools**：部分已實作（面試題、答案分析、MinIO 註冊）；缺職缺搜尋/推薦、SQL/RAG、 多模態
- **MCP Events**：未實作（缺 `event://*` 與事件註冊）；現有僅 Redis 事件紀錄未暴露為 MCP Event

### 功能面逐點對照
- **履歷處理（Resources）**
  - 現況：無 MCP 資源；僅 FastAPI 的示意上傳/解析 API
  - 證據：全倉無 `@mcp.resource`、`resource://*`
- **職缺推薦（Tools）**
  - 現況：無 `search_jobs(...)`、`recommend_jobs(...)`
- **面試互動（Events）**
  - 現況：無 MCP Event；僅 Redis 事件（`append_event`/`list_events`）
- **資料庫存取（Resources + Tools / RAG）**
  - 現況：有 Postgres/Redis/Mongo/MinIO 連線；未以 MCP 封裝 SQL 或向量檢索
- **多模態（Tools）**
  - 現況：無 `transcribe_audio(...)`、`analyze_resume_layout(...)`

### 介面草案（示例）
- **Resources**
  - `resource://resume/{id}`：取得履歷原檔（PDF/Word/CSV）
  - `resource://resume/{id}/parsed`：取得解析後結構化 JSON（姓名、經歷、技能）
  - `resource://db/job_embeddings`：職缺嵌入向量集合（可先 mock）
- **Tools**
  - `search_jobs(query: string)`
  - `recommend_jobs(resume_id: string, top_k: number = 5)`
  - `query_sql(sql: string)`（白名單/只讀）
  - `transcribe_audio(file_id: string)`、`analyze_resume_layout(file_id: string)`
- **Events**
  - `event://interview/start`、`event://interview/answer`、`event://interview/end`

### MVP 補強（按價值/成本排序）
- **優先級 A**
  - 履歷資源化：`resource://resume/{id}`、`resource://resume/{id}/parsed`
  - 面試事件：以 Redis 事件封裝為 MCP Events（start/answer/end）
- **優先級 B**
  - 職缺工具：`search_jobs`（Mongo/SQL LIKE/全文）與 `recommend_jobs`（以 parsed keywords/skills 打分）
- **優先級 C**
  - RAG：`query_sql`（只讀白名單）、`resource://db/job_embeddings`（先 mock，再接向量庫）
- **優先級 D**
  - 多模態：`transcribe_audio`、`analyze_resume_layout`（先佔位，後接 STT/OCR）

### 實作順序與影響
1) 新增履歷 Resources 與 MCP Events，不影響既有 `@mcp.tool()`
2) 實作 `search_jobs`/`recommend_jobs` 最小可用版（依現有資料層）
3) 補 `query_sql` 與向量資源（先 mock），再評估接入 pgvector/FAISS/Chroma 等
4) 多模態工具（依需求串接供應商或本地 OCR/STT）

### 後續計畫
- 將 `resource://db/job_embeddings` 連接至正式向量庫與定期更新流程
- 為 `query_sql` 建立白名單與 SQL 解析器，確保安全與可觀測
- 將面試事件串接前端，統一由 MCP Event 驅動回饋與狀態